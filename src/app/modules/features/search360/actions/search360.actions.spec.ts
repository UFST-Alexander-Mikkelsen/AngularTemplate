import * as Search360Actions from './search360.actions';

describe('Search360', () => {
  it('should create an instance', () => {
    expect(new Search360Actions.LoadSearch360s()).toBeTruthy();
  });
});
